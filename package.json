{
  "name": "namaaz-tracker",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "echo 'No build step needed for static site' && mkdir -p build && cp -r public/* build/ && cp -r src/* build/",
    "build": "echo 'No build step needed for static site' && mkdir -p build && cp -r public/* build/ && cp -r src/* build/"
  },
  "dependencies": {
    "firebase": "^12.11.0"
  }
}
