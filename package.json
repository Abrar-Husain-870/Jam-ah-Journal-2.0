{
  "name": "namaaz-tracker",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx serve public -p 8080",
    "vercel-build": "node build.js",
    "build": "node build.js"
  },
  "dependencies": {
    "firebase": "^12.11.0"
  }
}
